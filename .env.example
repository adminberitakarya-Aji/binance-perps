# Copy file ini ke .env dan isi nilainya. JANGAN commit .env ke git.

# --- Binance USDⓈ-M Futures API ---
# Buat API key di Binance: Profile → API Management → Create API
# Aktifkan permission: Futures (JANGAN aktifkan Withdraw)
# Untuk testnet: https://testnet.binancefuture.com → API Key (key terpisah dari mainnet)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# true = testnet (wajib dipakai dulu sebelum live), false = mainnet
BINANCE_USE_TESTNET=true

# Simbol yang diperdagangkan (format Binance USDⓈ-M Futures)
BINANCE_SYMBOL=BTCUSDT

# --- Alert Telegram (opsional; kosongkan keduanya untuk menonaktifkan) ---
# 1. Buat bot via @BotFather di Telegram → dapatkan token.
# 2. Kirim pesan apa pun ke bot kamu, lalu buka
#    https://api.telegram.org/bot<TOKEN>/getUpdates → cari "chat":{"id":...}
# 3. Isi kedua variabel di bawah. Uji kirim: python -m src.utils.notifier
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Filter ML (DEFAULT NONAKTIF -- aktifkan setelah model Binance lolos validasi) ---
# true = filter ML ON (butuh onnxruntime + model di models/)
ML_FILTER_ENABLED=false
# Ambang p(win) minimum agar sinyal dieksekusi (fail-closed)
ML_THRESHOLD=0.60
# Kosong = default models/btcusdt_ml_rf_1h.onnx
ML_MODEL_PATH=
